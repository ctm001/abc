/// Stub service for playing letter audio.
///
/// Replace the body of [playLetterSound] when an audio
/// package (e.g. `audioplayers` or `just_audio`) is added.
class AudioService {
  /// Plays the sound for the given [assetPath].
  ///
  /// Example: `playLetterSound('assets/audio/letters/a.mp3')`
  Future<void> playLetterSound(String assetPath) async {
    // TODO: implement with an audio package.
  }
}
